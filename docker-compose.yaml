version: '3.8'

services:
  pokemon-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./pokemon.db:/app/pokemon.db
    environment:
      - ENVIRONMENT=development
    restart: unless-stopped